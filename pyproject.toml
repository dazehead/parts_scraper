[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [
    ".",
    "gui_app",
    "scraper_app",
    "image_proc_app",
]
addopts = "-ra --strict-markers"
filterwarnings = [
    "ignore::DeprecationWarning:pandas",
]
